port: 8080

probes:
  - name: httpbin
    url: "https://httpbin.org/get"
    interval_seconds: 30
    timeout_ms: 5000
    retries: 2

  - name: example
    url: "https://example.com"
    interval_seconds: 60
    timeout_ms: 3000
    retries: 1
